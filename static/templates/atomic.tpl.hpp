/****************************** atomic model {model_number} *****************************************/
template<typename TIME>
class atomic_{model_number} : empty_atomic<ports, TIME> {{
	public:
    	atomic_{model_number}() = default;
}};
/**************************************************************************************************/
